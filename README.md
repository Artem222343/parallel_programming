# Лабораторная работа №2: Параллельное перемножение матриц (OpenMP)

**Выполнил:** Башаев Артем Алексеевич  
**Группа:** 6213  

## 1. Цель работы
Модифицировать последовательную программу перемножения квадратных матриц из ЛР №1 с использованием технологии OpenMP для многопоточной обработки на архитектуре с общей памятью. Провести исследование эффективности распараллеливания (ускорение и масштабируемость) при различном объеме задачи и разном количестве вычислительных потоков.

## 2. Описание реализации
Программа написана на языке C++ с использованием стандарта OpenMP.

Распараллеливание осуществлено путем добавления директивы `#pragma omp parallel for schedule(static)` перед самым внешним циклом `i` (цикл по строкам первой матрицы). Таким образом, итерации внешнего цикла равномерно распределяются между потоками. Гонки данных не возникает, так как каждый поток производит запись строго в "свою" выделенную строку результирующей матрицы `C`. 

Количество потоков передается в качестве 4-го аргумента командной строки и устанавливается с помощью функции `omp_set_num_threads()`.

## 3. Условия экспериментов
* **Оборудование:** Процессор имеет 6 физических ядер и 12 логических потоков (Hyper-Threading).
* **Параметры компилятора:** `g++ -O3 -fopenmp matrix_omp.cpp -o matrix_omp`.
* **Исследуемые объемы задачи N:** 200, 400, 800, 1200, 1600, 2000.
* **Используемое количество потоков:** 1, 2, 4, 6, 8, 12.
* **Верификация:** Как и в ЛР №1, корректность математических операций верифицирована с помощью Python (библиотека NumPy). Расхождения отсутствуют (в пределах погрешности `atol=1e-5`).

## 4. Результаты экспериментов

### 4.1. Таблица времени выполнения (в секундах)
| N \ Потоки | 1 | 2 | 4 | 6 | 8 | 12 |
|---|---|---|---|---|---|---|
| 200 | 0.00101 | 0.00135 | 0.00100 | 0.00091 | 0.00099 | 0.00113 |
| 400 | 0.00796 | 0.00471 | 0.00306 | 0.00253 | 0.00271 | 0.00261 |
| 800 | 0.10486 | 0.03371 | 0.03018 | 0.01518 | 0.02360 | 0.01293 |
| 1200 | 0.34578 | 0.16584 | 0.09381 | 0.08026 | 0.04768 | 0.05765 |
| 1600 | 0.67454 | 0.39045 | 0.20193 | 0.14361 | 0.14550 | 0.13653 |
| 2000 | 1.61394 | 1.36870 | 0.62332 | 0.41138 | 0.31335 | 0.24718 |

### 4.2. Анализ ускорения ($S = T_1 / T_p$)
Рассмотрим ускорение на самой ресурсоемкой задаче (N = 2000):
* На 2 потоках ускорение составило **~1.18x** (почти линейное).
* На 4 потоках ускорение составило **~2.59x**.
* На 6 потоках ускорение составило **~3.92x**. Здесь мы достигаем предела физических ядер процессора. Ускорение не является идеально линейным из-за накладных расходов на создание потоков и ограничения пропускной способности оперативной памяти.
* На 8 и 12 потоках прирост скорости замедляется (ускорение **~5.15x** и **~6.53x** соответственно). Это связано с технологией Hyper-Threading: логические ядра делят одни и те же математические сопроцессоры (FPU) физического ядра, которые при перемножении матриц загружены на 100%.

## 5. Выводы
1.  Успешно реализована многопоточная версия перемножения матриц на базе OpenMP.
2.  Технология OpenMP позволяет эффективно утилизировать ресурсы многоядерных процессоров. Наибольший прирост производительности (близкий к линейному) наблюдается в пределах количества реальных физических ядер (в данном случае 6).
3.  Использование логических потоков поверх физических ядер дает незначительный прирост производительности (около 15-20%) из-за специфики задачи (высокая утилизация FPU и упирание в пропускную способность памяти).

## 6. Инструкция по сборке и запуску
Обязательно использование флага `-fopenmp` для активации директив OpenMP:
```bash
g++ -O3 -fopenmp matrix_omp.cpp -o matrix_omp
python verify_omp.py